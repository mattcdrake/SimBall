/*
	SimBall by Matt Drake

    This is the entry point for Simball, containing the SDL code to open and
	initialize a window.
*/

#include <SDL.h>
#include <SDL_image.h>
#include <stdio.h>
#include <string>

const int SCREEN_WIDTH = 640;
const int SCREEN_HEIGHT = 480;

// Creates a window using SDL
bool init();

// Close window and free up resources
void close();

bool init()
{

}

void close()
{

}

int main(int argc, char* args[])
{
	return 0;
}
